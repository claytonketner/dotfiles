[user]
	name = clayton
	email = clayton@counsyl.com
[branch]
	autosetuprebase = always
[push]
	default = simple  # For git 1.7, use upstream
[alias]
	s = status
    co = checkout
    br = branch
    # Open all merge conflicts in vim
    fix = "!f() { vim -p `git diff --name-only --diff-filter=U`; }; f"
[color]
	ui = true
[core]
	excludesfile = ~/.gitignore_shared
