[user]
	name = clayton
	email = clayton@counsyl.com
[branch]
	autosetuprebase = always
[push]
	default = simple
[alias]
    st = status
    co = checkout
    ci = commit
    br = branch
    com = checkout master
    d = diff
    ds = diff --staged
    # Open all merge conflicts in vim
    fix = "!f() { vim -p `git diff --name-only --diff-filter=U`; }; f"
[color]
	ui = true
[core]
	excludesfile = ~/dots/.gitignore_shared
